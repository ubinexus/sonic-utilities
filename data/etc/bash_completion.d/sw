_sw_completion() {
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _SW_COMPLETE=complete $1 ) )
    return 0
}

complete -F _sw_completion -o default sw;
